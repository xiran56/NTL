#pragma once

#include <limits>

#include "impl/digits.hh"

namespace ntl {
    template<class View, class ViewTraits>
    class DigitsIterator {
        static constexpr std::size_t UNDEFINED { std::numeric_limits<std::size_t>::max() };
    public:
        
    };
}
