#pragma once

#include "Helpers.hh"
#include <type_traits>

namespace ntl::impl {
    template<class View>
    class DigitViewTraits {
    public:
        DigitViewTraits() = delete;

        ~DigitViewTraits() = delete;

        
    };
}
